<UserControl x:Class="MoodMovies.Views.MovieListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MoodMovies.Views"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Column="0" Grid.Row="0" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="1" Grid.Row="0" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="2" Grid.Row="0" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="3" Grid.Row="0" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="0" Grid.Row="1" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="1" Grid.Row="1" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="2" Grid.Row="1" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock Text="{Binding Test}" Style="{DynamicResource MaterialDesignTitleTextBlock}" TextAlignment="Center"/>
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="3" Grid.Row="1" Padding="10" Margin="10" materialDesign:ShadowAssist.ShadowDepth="Depth5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image />
        </materialDesign:Card>
                
    </Grid>
</UserControl>
